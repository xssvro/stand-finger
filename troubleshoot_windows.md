# Windows无法识别设备 - 故障排除

## 重要提示

**运行 `usb-gadget.sh` 脚本后，不需要重启树莓派**。USB Gadget会立即生效。

## 快速检查清单

### 1. 在树莓派上检查（通过SSH）

```bash
# 检查设备文件是否存在
find /dev -name "hidg*"
# 应该看到 /dev/hidg0 和 /dev/hidg1

# 检查UDC是否启用
cat /sys/kernel/config/usb_gadget/pi4/UDC
# 如果有输出（如：20980000.usb），说明已启用
# 如果是空的，说明未启用，需要重新运行脚本
```

### 2. 检查USB连接

- ✅ **使用数据线**（不是充电线）
- ✅ **连接到USB数据口**（靠近HDMI接口）
- ✅ **连接到Windows电脑的USB口**

### 3. 重新插拔USB（重要！）

1. **断开**树莓派与Windows电脑的USB连接
2. **等待3-5秒**
3. **重新连接**
4. Windows会自动检测新设备

### 4. 在Windows上操作

1. **打开设备管理器**：
   - 按 `Win + X`
   - 选择"设备管理器"

2. **刷新设备列表**：
   - 点击"操作"菜单
   - 选择"扫描检测硬件改动"
   - 或按 `F5` 刷新

3. **检查多个位置**：
   - **键盘** → 应该看到 "Raspberry Pi USB HID Keyboard Mouse"
   - **鼠标和其他指针设备** → 应该看到 "Raspberry Pi USB HID Keyboard Mouse"
   - **人体学输入设备** → 可能在这里
   - **通用串行总线控制器** → 如果有未知设备，可能在这里

## 详细排查步骤

### 步骤1：确认脚本运行成功

在树莓派上运行：

```bash
# 运行检查脚本
./check_usb_connection.sh

# 或手动检查
cat /sys/kernel/config/usb_gadget/pi4/UDC
ls -l /dev/hidg*
```

**预期结果**：
- UDC有输出（不是空的）
- `/dev/hidg0` 和 `/dev/hidg1` 存在

### 步骤2：确认USB连接正确

**树莓派Zero接口位置**：
```
树莓派Zero (俯视图)
┌─────────────────────────┐
│  [HDMI]                 │
│  [USB数据口] ← 用这个！ │
│  [GPIO]                 │
│  [SD卡槽]               │
│  [电源接口] ← 不能用    │
└─────────────────────────┘
```

**检查清单**：
- [ ] 使用的是数据线（能传输数据，不只是供电）
- [ ] 连接到USB数据口（靠近HDMI）
- [ ] 另一端连接到Windows电脑的USB口

### 步骤3：重新插拔USB连接

**为什么需要重新插拔？**
- Windows需要检测新的USB设备
- 重新连接会触发设备枚举
- 这是最常用的解决方法

**操作步骤**：
1. 在Windows设备管理器中打开
2. 断开USB连接
3. 等待3-5秒
4. 重新连接
5. 观察设备管理器是否有变化

### 步骤4：检查Windows设备管理器

**可能的位置**：

1. **正常位置**：
   - 键盘 → "Raspberry Pi USB HID Keyboard Mouse"
   - 鼠标和其他指针设备 → "Raspberry Pi USB HID Keyboard Mouse"

2. **其他可能位置**：
   - 人体学输入设备 → 可能在这里
   - 通用串行总线控制器 → 如果有未知设备

3. **如果有黄色感叹号**：
   - 右键点击设备
   - 选择"更新驱动程序"
   - 选择"自动搜索驱动程序"
   - 通常不需要额外驱动，Windows会自动识别

### 步骤5：如果仍然看不到

**在树莓派上重新运行配置**：

```bash
# 1. 检查UDC状态
cat /sys/kernel/config/usb_gadget/pi4/UDC

# 2. 如果UDC为空，重新运行脚本
sudo ./usb-gadget.sh

# 3. 再次检查
cat /sys/kernel/config/usb_gadget/pi4/UDC
find /dev -name "hidg*"
```

**在Windows上**：
1. 完全断开USB连接
2. 等待10秒
3. 重新连接
4. 刷新设备管理器

## 常见问题

### Q1: 运行脚本后需要重启吗？

**A**: 不需要！USB Gadget会立即生效。只需要重新插拔USB连接。

### Q2: 设备管理器中没有看到设备？

**A**: 
1. 检查树莓派上UDC是否启用
2. 确认使用数据线和正确的USB口
3. 重新插拔USB连接
4. 刷新设备管理器

### Q3: 看到未知设备或黄色感叹号？

**A**: 
- 可能是驱动问题，但通常不需要额外驱动
- 右键 → 更新驱动程序 → 自动搜索
- 或者卸载设备后重新插拔

### Q4: 设备管理器中有设备但名称不对？

**A**: 这是正常的，只要设备能工作就可以。名称取决于USB描述符。

## 验证设备是否工作

即使设备管理器中没有显示，也可以测试设备是否工作：

1. **在树莓派上运行服务**：
   ```bash
   sudo python3 main.py
   ```

2. **发送测试命令**：
   ```bash
   curl -X POST http://树莓派IP:5000/api/test
   ```

3. **观察Windows**：
   - 鼠标应该会移动并画圆
   - 如果鼠标移动了，说明设备工作正常

## 总结

**关键步骤**：
1. ✅ 运行脚本后**不需要重启**树莓派
2. ✅ 确认使用**数据线**和**正确的USB口**
3. ✅ **重新插拔USB连接**（最重要！）
4. ✅ 在Windows上**刷新设备管理器**
5. ✅ 检查**多个位置**（键盘、鼠标、人体学输入设备等）

如果按照以上步骤操作后仍然看不到设备，请检查树莓派上的配置是否正确。
